🌐 語言：[English](./CHANGELOG.md) | [繁體中文](./CHANGELOG-TW.md)

# 更新紀錄

Bynálix 每一版的發佈紀錄，由新到舊。應用程式本身的介紹與安裝方式請見
[README](./README-TW.md)。

---

### v1.6.3
- **不再畫「衍生量之間的當日相關」** — 一天的熱量就是（近似）4×蛋白質 + 4×碳水 + 9×脂肪，所以「碳水高的那幾天熱量也高」是加法，不是行為。在真實的數月匯出資料上，這三條恆等式是整張圖裡**最強**的連線（r = 0.87、0.75、0.67），把真正有意義的關聯全擠掉了。現在只拿掉它們的當日對齊，延遲的問題照樣會問 —— 其中兩條以相反符號回到圖上，成為真正的「延遲 1 天」發現：吃多的隔天吃得少，這個補償模式原本被恆等式蓋住了。運動消耗對運動時長同理，因為消耗本來就是由時長推估的
- **關係線不再從其他變因點底下穿過** — 線鑽進節點底下，看起來像是在那裡結束，或像那個節點也是這段關係的一部分。現在每條線的弧度都會對照它沒有連到的每一個節點檢查，必要時加大弧度或改往另一側繞，本來就沒擋到的維持原樣。同一份資料上，會穿過節點的線從 3 條降到 0 條
- **輸出圖片時文字不再跑位** — 截圖函式庫量測字型基線時，用的隱藏探針繼承了頁面的行高（24px），而不是被截元素自己的行高（小字是 14px），導致每個圖例與說明文字都被畫在瀏覽器位置下方約 6px 處，色塊和標籤因此分家。改用 html2canvas-pro，它直接從 canvas 自己的字型度量取基線；已驗證與畫面上的 DOM 完全一致
- **Android：輸出的圖表現在真的會出現在相簿裡** — 只把檔案寫到磁碟，相簿 App 是看不到的：它們列出的是 MediaStore 裡登記的內容，而不是檔案系統上的東西。現在改成透過 MediaStore 本身寫入（Android 10 以上，存到 Pictures/Bynalix），舊版則寫檔後主動通知媒體掃描器；原本的純檔案寫入保留為後備

### v1.6.2
- **所有圖表都可以輸出成圖片** — 每張圖表卡片上都有輸出按鈕，會把整張卡片存成 JPEG，並在右下角壓上輸出當天的日期浮水印。Android 直接存進相簿資料夾（失敗時退回 Documents/Bynalix/image），桌面則跳出系統的另存新檔。涵蓋歷史分頁（體重、熱量、每日餐次、三大營養素、飲水、睡眠、訓練量、單項最大重量、運動分布、時長與消耗、每日活動消耗、身體組成、睡眠記錄）與統計分頁（相關係數排名、關係網絡圖、關係變化、穩定星圖），總覽分頁依需求不提供。輸出的是畫面上「當下那張圖」而不是重新畫一張，所以區間、語言、狀態都跟你看到的完全一致；按鈕本身也不會出現在自己輸出的圖裡
- **關係圖新增兩個用餐時間變因** — 由當天的用餐時間推導出**進食窗口**（最後一餐減第一餐）與**最長餐距**（當天連續兩餐之間最長的一段間隔），兩者都是以小時為單位的當日數值，凌晨 4 點前的用餐一樣沿用既有「最後用餐時間」的跨日處理方式。當天只有一餐時兩者都記為 0 —— 用真實的數月匯出資料實測過：這個約定會把平均值往下拉不到 0.1 小時，且沒有任何一條連線因此跨過顯著性門檻，屬於真實存在但低於門檻的失真，不是看不見的黑箱
- **關係變化現在會說明「測不出來」，不再讓人誤以為是「沒有發現」** — 前後分段檢定需要在切點兩側各有足夠長的一段記錄，所以記錄不足 90 天時根本產生不出任何候選切點，這趟掃描結構上必然回報空的。過去這個情況會顯示「這段區間沒有找到新出現且持續的關聯」，聽起來像是做完檢定後的結論，但檢定其實從未執行。現在在按下分析之前就會先檢查區間並說明為什麼需要 90 天；若分析後確實一個切點都測不到，也會明白寫出來
- **穩定星圖的說明改放到下方獨立卡片** — 原本擠在圖表上方的大段文字獨立成一張卡片，分成分數怎麼算（附 0／0.5／1.0 → 100／50／25 的對照）、資料不足的項目會怎麼呈現、下方表格每一欄是什麼三個區塊，並預設收起 —— 這是參考資料，不需要每次進來都重看一遍
- **穩定星圖拿掉密度三角色塊** — 標示「資料不足」的彩色三角區塊會跟星形本身搶注意力，而它想表達的資料密度在正下方的表格裡本來就有一欄。現在資料不足的項目改用「軸標籤變淡＋星形直接跨過該角」表示，實際密度就留在讀起來最清楚的地方：表格右側的色點
- **飲食搜尋換頁時會捲回最上方** — 之前在分類裡換頁，清單會停留在上一頁捲到的位置，新一頁的前幾筆一開始看不到

### v1.6.1
- **時間線改為穩定度** — 原本「目標長期／新出現影響」的時間線分頁整個拿掉，改成一張星圖，評估體重、熱量、三大營養素、飲水、睡眠這七項各自的日對日一致程度，跟其他變因的相關性無關。運算方式沿用網絡圖本身的去趨勢技巧（日對日一階差分），而非直接算「距離平均值的變異」，所以像穩定減脂中的體重持續下降這種正常趨勢，並不會被算成「不穩定」——只有趨勢之外的那段波動才算數。相對波動程度（日波動的標準差除以該項目自己的平均值）換算成 0–100 分，每超過一個校準基準點的波動幅度分數就減半；這個基準點是拿一份真實的數月匯出資料實測校準出來的：像體重這種物理上變化緩慢的項目，數值天生就比每天靠選擇決定的項目（熱量、三大營養素、飲水、睡眠）低了兩個數量級，這是真實現象、不是bug，基準點就是為了讓這群數值落在量表中段，而不是全部擠在極端
- **算不出來的地方會明確標示，不會硬算** — 每項的日對日波動只用「連續兩天都有記錄」的資料算，中間斷一天就跳過那一段銜接，不會補 0 或內插。當這段區間內記錄天數低於 50%（跟其他統計分頁畫紅／黃／綠密度線用的門檻完全一樣）時，該項目就回報「資料不足」而不硬算分數，星圖上對應的那塊三角區域也改畫成純色的密度色塊，不會讓圖表悄悄暗示一個其實不存在的分數
- **詳細數值排在下方，版面跟規律分頁的關聯清單一樣** — 每一列列出平均值、以該項目自己單位表示的日波動、樣本數，以及一個密度色點
- **沿用頁面共用的日期區間** — 跟基礎／進階／規律分頁一樣，穩定度直接用 Statistics 頁面最上方已選好的區間自動運算，不再像時間線那樣需要另外一張「最寬起點」的設定卡

### v1.6.0
- **時間線改為圍繞「目標」設計** — 時間線分頁不再顯示關係網絡圖；縮短區間的分析改為純後端運算，計算對象固定為你的目標變因（體重，方向依你的模式而定），不再畫成圖。改用兩種結果取代：**長期存在的影響** — 不論分析窗口拉長或縮短都成立，包含最近、最緊縮的一個窗口在內，因此不是舊資料造成的錯覺，現在也還在發生；**新出現的影響** — 與目標的關聯是從記錄中途才開始成立、且持續到最新資料的因子，列出起始日期、前後兩段的 r 與樣本數，以及已對整趟搜尋校正過的 p 值。兩者都再分為正面（有助目標）與負面（不利目標）
- **補上單看畫面看不出來的情況** — 夠強的關聯即使被舊資料稀釋也不會從逐張畫面中消失，因此永遠不會「出現」；唯有把記錄切成前後兩段才看得出它實際的起始點。已用刻意埋入行為轉變的模擬資料驗證：回報的起始日期與真實轉折相差在一天以內，而純雜訊資料在數十次模擬中只有極少數會誤判出「新出現」的關聯
- **長期存在的影響改看趨勢，不只是一個數字** — 每個關聯都會再算一次 Spearman 相關係數，這次算的是「該關聯的 r 值」隨窗口位置變化的走勢（沿用網絡本身的等級相關運算，只是往上提一層，改看一個相關係數自己的軌跡，而非兩個原始變因），結果分類為增強中、減弱中或持平。呈現方式改成波形圖，不再只用一個標籤斷言：逐窗口的原始 r 值畫成細線，疊上一條移動平均後的「包絡」趨勢線 —— 真正在增強或減弱的關係，看起來就會是一條往上或往下走的線。已用手動建構、走勢單調已知的窗口序列驗證（刻意跳過完整模擬會引入的窗口混合效應，只測趨勢統計本身）：兩個方向都以 r > 0.97、p 遠低於 1e-90 的結果正確辨識，持平的序列也正確判讀為「持平」
- **新出現的影響現在也能看出不只一次轉折** — 找到起始日期後，會在更早的「起始前」那段區間內再做一次不限方向的搜尋，檢查那段本身是否也藏著更早的轉折（例如：先是中等強度、接著轉淡、後來又變得更強），而不是單純一次到位的開關。找到的話，圖表會畫成階梯狀 —— 每段對應一個 r 值，在每次轉折處階梯狀跳動，並在邊界標示虛線；大多數關聯仍然只會落成原本的前後兩段。已用刻意校準的三段式模擬情境驗證（強度依序為無感 → 中等 → 強，同號延續，確保「最近」始終是最強的一段，符合主搜尋的前提）：三段區間與兩個邊界日期都被正確找回，整趟搜尋校正過的 p 值也遠低於門檻；在 40 次純雜訊模擬中，沒有出現任何一次誤判的三段式結果
- **整段走勢圖改版** — 原本的連線數／平均 |r| 已不再符合新功能，現在改畫每個分析窗口找到幾個正面／負面的目標相關因子；把游標移到柱狀上可看是哪些因子
- **計算成本降低約 7 倍** — 每個窗口只對單一固定的目標變因評分，而非整個成對網絡，每張分析所需計算的相關性從約 90 組降到約 13 組
- **區間選擇改用與其他分頁一致的模型** — 分析區間的選法改成跟「基礎／進階／規律」分頁一樣：預設天數快捷鍵，或自訂日期範圍，直接沿用同一個日期範圍元件。快捷鍵天數依時間線的需求調整（全部歷史／365／180／90 天，皆固定結束於最新資料），而非其他分頁的 14／30／90 天 —— 縮短區間的分析需要足夠長的起始區間才有意義；自訂範圍現在也能自行選擇終點，不再只能選起點，若選到短於 15 天（相關性成立的最低門檻）會提示
- **改為自動計算，與其他分頁一致** — 過去選好區間後還要另外按「開始分析」才會有結果；現在區間一確定就自動算出結果，改選其他區間也會自動重新計算，跟基礎／進階／規律分頁「不用按按鈕，直接看到預設結果」的模式一致。手動的「分析精細度」間隔選擇器也拿掉了：改成以目標變因為中心運算後成本降低約 7 倍，一年以內的區間用單日間隔本身就在成本預算內；而既有的自動放寬機制（總窗口數與總分析天數都設有上限）本來就會在區間偏長時自動選用較粗的間隔 —— 這個手動選項實際上只讓人「刻意選得比必要粗」，並非真正需要開放的選擇。仍保留手動重新整理按鈕，可在不改變區間的情況下強制重算
- **關係變化（規律分頁）** — 關係網絡圖下方新增「分析」區塊，回答網絡圖本身答不出來的問題：這條連線是一直都存在，還是最近才出現的？把整個成對網絡（不限定單一目標變因）沿用時間線分頁的「從長到短」窗口序列重新評分，只保留「記錄中途才開始成立、且持續到最新資料」的關聯，並排除整段歷史都成立的關係（體重與熱量這類長期穩定關係依設計不會列入）。留下來的關聯依「共同因子」分組成卡片 —— 挑出連結數最多的變因作為卡片主角，用多線圖呈現（X 為窗口長度、Y 為相關係數，每個相關因子一條線，並標示最早的起始日期虛線），只跟一個因子有關聯的不會單獨成卡。不是每次分析都會有結果 —— 這是資料裡真實發生的事件，不是必然存在的東西。已用刻意埋入三方共同新出現關聯的模擬資料驗證（回報的起始日期都與真實轉折相差數天以內），並在純雜訊資料上測試（在把顯著性門檻收緊以因應約 90 組配對的搜尋規模，遠大於時間線目標導向掃描的約 13 組後，約每 30 次分析才會有 1 次誤判）
- **修正 — 自訂目標的熱量圖表對不上實際目標** — 歷史分頁的每日熱量圖表，對自訂營養目標的使用者一直悄悄回退成寫死的 2000 kcal，因為那裡的目標計算漏傳了總覽分頁本來就有正確傳入的兩個參數；目標參考線與標籤在這個模式下從未顯示過真正的目標。現在兩個分頁的算法已一致
- **修正 — 瀏覽食物分類會漏掉排在第 30 名之後的項目** — 選擇分類（或蛋白質這類聚合標籤）時，清單依名稱排序後只取前 30 筆，排在之後的項目即使確實存在資料庫裡也永遠不會顯示 —— 用真實備份資料驗證過：一個 43 筆的分類裡，兩個項目分別排在第 40、41 名。已把這個寫死的上限改成「上一頁／下一頁」分頁，不論分類多大都能瀏覽到每一筆

### v1.5.0
- **新增「時間線」分頁** — 規律隔壁的新分頁，把變因關係網絡做成連續幻燈片。選定起點 A 後，終點固定在最新一筆資料，每張圖把起點往前推一段：A~現在、A+1~現在、A+2~現在……一路縮到相關性還站得住的最短區間（15 天）。連續放映就能看出哪些關聯在舊資料退場後仍然成立，哪些其實只存在於那段舊資料裡。間隔預設 1 天（可選 1／2／3／7／14 天），整段播放長度可設 5、10、20、40 秒，並有播放／暫停、單張前後、循環與進度拖曳
- **整段共用同一組節點位置** — 環形排列由「所有張數的聯集」一次算出，因此每個變因在整段時間線上固定在同一個位置，畫面上會移動的只有連線本身。當張圖沒有關聯的變因會原地淡出，而不是被移走，避免看起來像整張圖在重排
- **每張圖的區間變化** — 每張圖列出相對前一張新增、消失與強度改變的連線，並標示正負轉向與領先／延遲天數的改變；另外持續與最寬的第 1 張比較：累計新增、消失幾條，平均 |r| 移動多少
- **變化起點** — 回答「這個關聯是從什麼時候開始的」，而不只是「它存在」。把記錄從每一個候選日期切成前後兩段，用該日之後的相關性與該日之前的相關性互相檢定（Fisher z 的差異檢定，因此某一側只剩一小撮資料的切法不會被誤當成發現）。回報前後差距最大的那一天，並同時列出兩段的 r、樣本數與天數：綠色代表該關聯從那天起、直到最新資料才成立，紅色代表它只在那天之前的資料裡成立。點一下該列即可跳到當天的關係圖。由於每組配對都試過所有切點並取最佳，p 值會針對整趟搜尋做 Bonferroni 校正 — 在刻意埋入行為轉變的模擬資料上，回報日期與真實轉折相差數天以內；純雜訊資料則通常什麼都不會列出。它也補上了單看幻燈片看不出來的情況：夠強的關聯即使被舊資料稀釋仍然畫得出來，因此逐張看永遠不會「出現」，唯有切開前後才看得見它的起點
- **整段走勢圖與連線持續度** — 以區間長度為 X 軸畫出連線數與平均 |r|（點一下即可跳到該張），並為每組變因配對畫出色帶，顯示它在哪些區間成立、以及在整段時間線中存在的比例。100% 代表無論區間怎麼縮短都成立；只出現在左側則代表那段關聯來自較早的資料
- **計算量有上限** — 張數與「整段分析的總天數」皆設上限，跨年度的長區間會自動放寬間隔，而不是跑上好幾分鐘。產生過程分段執行，有進度條並可中途取消
- **關係網絡計算加速約 12 倍** — 差分與延遲對位原本會為「每個變因、每個值、每個延遲」都建立並重新格式化一次 `Date`，成本反而蓋過統計本身。現在日期一次轉成整數，並以「一天一格」的稠密陣列存放，排名改用共用緩衝區而非每次配置暫存陣列，p 值也只為最後勝出的那一組計算，而不是七組都算。輸出完全不變 — 已在不同區間長度、含缺漏的資料與各種選項下逐一比對確認一致
- **節點過多時的標籤可讀性** — 環上超過 12 個變因時，標籤會略為縮小，且上下兩端的標籤改為交錯外推，相鄰的名稱不再互相重疊

### v1.4.5
- **飲水達標彈性化** — 近 7 天達標率卡片的飲水指標，改用跟熱量一樣的目標 80–110% 彈性區間，取代原本單邊「≥90%、無上限」的判定
- **新增營養均衡指標** — 近 7 天達標率卡片在「熱量」下方新增一列：當天蛋白質、碳水、脂肪都落在各自目標的 80–110% 內才算達標，不只看總熱量
- **趨勢線預設開啟** — 歷史頁面總覽圖表的趨勢線開關預設改為開啟，啟用時按鈕會顯示綠色勾勾

### v1.4.4
- **時段變因** — 變因關係網絡新增「最晚進食」「運動時段」「起床時間」，讓進食與訓練的「時間點」也能與體重、睡眠、攝取做相關分析。跨午夜的最晚進食會正確排在 23:00 之後而非繞回最早；時段與時段之間的配對因必然相關而略過，每個時段變因也只保留最強的三條關聯
- **NEAT／TDEE 更穩定** — 自動活動等級原本只看「過去 7 天」的訓練天數，因此單一個安靜的一週就可能讓人從「高度活躍」直接掉到「久坐」，TDEE 一夜之間變動約 840 kcal。現在視窗會隨可用資料成長（最短一週，最長四週）並依近期程度加權，休息一週只降一級而非四級，而真正的習慣改變仍會在一兩週內反映。另外不再要求必須在星期日開啟 App 才會更新，且資料不足一週時不會覆寫使用者手動設定的等級
- **總覽圖表趨勢線** — 體重、熱量、飲水、睡眠可切換顯示橘色虛線擬合，並標示每週斜率與採用的有效天數。X 軸採「日期間隔」而非陣列索引，因此中間缺資料不會扭曲斜率；未記錄而顯示為 0 的日子（飲食、飲水）會排除在擬合外，不會把趨勢往下拉。至少 7 天有效資料才會畫線
- **圖示按鈕加大並置中** — 重訓組數與飲食項目編輯中的確認／取消鈕原本只有約 21px，且圖示偏上（`<svg>` 預設對齊文字基線）。新增 `.icon-btn` / `.icon-btn-lg`，以 flex 置中的 36px / 44px 觸控範圍取代，並套用到全 App 53 個純圖示按鈕
- **進階統計 — 重訓** — 新增「週頻率」（過去 7 天的訓練天數），並新增明確的「全部位」選項，使訓練量、最大重量、週頻率都能選擇單一部位或所有部位綜合。先前必須指定部位或動作才能確認設定
- **歷史 — 每日飲水量** — 於熱量圖表與睡眠之間新增飲水量圖。目標虛線與達標著色依個人設定的飲水目標（體重 × ml/kg）而定；未設定目標時兩者都不顯示，不會用一個你沒選過的標準來評斷

### v1.4.3
- **訓練頻率變因** — 變因關係網絡新增「重訓頻率」與「有氧頻率」，皆以「過去 7 天的訓練天數」計算（有氧含一般運動項目）。頻率本身因此能與體重、睡眠、攝取等變因做相關分析，不再只有單次訓練量。未記錄的日子視為未訓練；區間前 6 天因視窗未滿而略過；若該區間完全沒有訓練紀錄，該變因會整個不顯示
- **變因關係網絡版面** — 無顯著關聯的變因不再畫在圓環上，改以精簡標籤列於圖表下方；圓環只保留「真的有結果」的變因，其餘節點也因此獲得更多空間
- **減少交叉** — 節點位置不再是固定的依領域排序圓環。領域仍維持分組，但排列改由窮舉搜尋挑選交叉最少的順序（以代表性資料實測：交叉 13 → 6）
- **圖例精簡** — 移除變因關係網絡的領域色點圖例；圓環上出現哪些領域取決於當次有沒有結果，色點圖例會隨之變動。只保留連線圖例（正相關／負相關／延遲影響）
- **線條優化** — 線條變細，並改為垂直於弦向外弓起，不再全部往圓心彎曲，重疊的連線得以分離成各自的弧線；過長的標籤也不再被切掉

### v1.4.2
- **規律頁** — 移除 Z-score 標準化趨勢圖（已由趨勢箭頭與變因關係網絡取代）
- **變因關係網絡** — 修正延遲影響箭頭被節點擋住的問題；連線改為收在節點邊緣外，方向箭頭完整顯示
- **睡眠偵測** — 改以整晚「最長螢幕關閉區間」推算睡眠，取代原本只抓「最後一次螢幕關閉」的做法。入睡與起床時間都以實際螢幕活動推算，因此關掉鬧鐘後晚點才登記也不會失準；起床時間改用偵測到的起床時刻預填，而非開啟表單當下的時間

### v1.4.0
- **自訂食物** — 支援編輯既有自訂食物（鉛筆按鈕）；營養素輸入不再限制 100 g 上限
- **重訓熱量改版** — 逐組計算：MET 依「舉重/體重比」分級（3.5 / 5.0 / 6.5）；次數決定出力時間，休息時間以 2.0 MET 計；組間休息可手填或自動偵測（以上一組登記時間推算，綠色顯示）
- **總覽** — 運動消耗納入重訓估算（原本僅計入有氧）
- **規律發現** — 統計「規律」頁新增變因關係網絡圖：以每日變化量計算 Spearman 相關，通過顯著性檢定（p < 0.05）才成邊；延遲影響以虛線箭頭標示方向；點節點聚焦、點連線看散點圖
- **週間規律** — 週末 vs 平日效應卡片（例如「週末熱量比平日高 12%」），經 Welch t 檢定過濾
- **CI 修正** — 移除已棄用的 tsconfig `baseUrl`（此設定錯誤先前導致型別檢查被靜默中止）

### v1.3.0
- **睡眠表單改版** — 起床時間與時長改用時鐘選擇器；起床時間預設為開啟當下時間；起床時間顯示在時長上方；睡眠資訊卡片放大，以大字上下並排顯示起床時間與時長
- **睡眠編輯 Modal** — 編輯改為跳出表單（Dialog），不再在卡片內展開
- **睡眠預測** — Android 整合 UsageStatsManager，讀取系統螢幕使用記錄推算入睡時間（無需背景服務）；開啟「新增睡眠」時自動計算時長並顯示綠色預測提示；首次使用時以橘色引導授權
- **時鐘選擇器一致** — 餐點時間與飲水時間的選擇器加上文字標籤，與睡眠表單樣式統一

### v1.2.1
- **Bug 修正** — TDEE 活動乘數修正為 Harris-Benedict 標準（1.2–1.9×）；原本低估約 10–50%
- **營養素** — 模式設定新增脂肪熱量比例 slider（佔 TDEE 的 25–35%，預設 30%）；公式改為脂肪 = TDEE × 比例，碳水補足剩餘熱量
- **增肌速率** — 調整為保守數值：緩慢 0.10%、標準 0.15%、積極 0.25% 體重/週
- **體脂** — 個人頁面的體脂欄位初始化後鎖定，後續需透過體態測量或體重紀錄更新
- **體脂過期提醒** — 每個門檻只通知一次（30 / 45 / 75 / 105 / 135 / 165 / 180 天），體重列於 30–179 天顯示 ⚠️，180 天以上顯示紅色半透明
- **睡眠** — 新增起床時間欄位，自動回推入睡時間（起床時間 − 睡眠時長）
- **程式品質** — Statistics 頁面的空 catch 區塊改為 logError

### v1.2.0
- **程式架構** — 大規模內部重構：建立元件庫（EmptyState、StickyHeader、PillButton、CardHeader、Modal/Dialog/BottomSheet）、設計 token 系統（間距、字型），以及樣式集中管理
- **Bug 修正** — 有氧歷史紀錄現在可正確納入飛輪項目
- **CI/CD** — 透過 GitHub Actions 建立全平台自動化發布流程（Windows / macOS / iOS / Android）

### v1.1.0
- **趨勢分析頁** — Z-score 標準化趨勢折線圖（體重 / 睡眠 / 熱量），自適應斜率偵測與方向指示
- **延遲分析** — 三組 Lag 分析，分別對應體重、睡眠、熱量的變化
- **可靠性優化** — 改用 Robust Z-score（中位數 + MAD），最低有效資料門檻降為 7 天
- **基礎統計** — 新增相關係數排行 BarChart（比照進階頁）
- **歷史圖表** — 有氧時長與熱量消耗改用雙 Y 軸
- **Bug 修正** — 身體歷史 Drawer 背景透明問題修復
